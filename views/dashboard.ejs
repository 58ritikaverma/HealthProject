


<!--  -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Dashboard</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #a6ddf0;
            margin: 0;
            padding: 0;
        }
        .container {
            width: 80%;
            margin: 20px auto;
            background-color: #b6e4ef;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 0 60px rgba(0, 0, 0, 0.1);
        }
        h2 {
            text-align: center;
            margin-bottom: 20px;
            color: #333;
            font-family: cursive;
            font-size: 2rem;
        }
        a {
            display: inline-block;
            text-decoration: none;
            padding: 10px 20px;
            background-color: #2ab5b2;
            color: white;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        a:hover {
            background-color: #206978;
        }
        .cards-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
        }
        .card {
            background-color: #dbe9ee;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s;
        }
        .card:hover {
            transform: translateY(-5px);
            background-color: #e9f5fa;
        }
        .card h3 {
            margin: 0;
            color: #333;
            font-size: 1.5rem;
            font-family: cursive;
        }
        .card p {
            margin: 10px 0;
            color: #555;
        }
        .btnn {
            display: block;
            width: fit-content;
            margin: 10px auto;
            padding: 10px;
            color: white;
            border: none;
            border-radius: 5px;
            text-align: center;
            font-family: cursive;
            font-size: 1rem;
            background-color: transparent;
        }
        .btnn a {
            text-decoration: none;
        }
        .welcome {
            margin-top: 20px;
            text-align: center;
            font-size: 1rem;
            color: #333;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Patient List</h2>
        <a href="/patients/add">Add New Patient</a>
        <div class="cards-container">
            <% patients.forEach(patient => { %>
            <div class="card">
                <h3><%= patient.name ? patient.name : 'N/A' %></h3>
                <p><strong>Age:</strong> <%= patient.age %></p>
                <p><strong>Gender:</strong> <%= patient.gender %></p>
                <p><strong>Symptoms:</strong> <%= patient.symptoms %></p>
                <p><strong>Date of Visit:</strong> <%= patient.dateOfVisit.toDateString() %></p>
            </div>
            <% }) %>
        </div>
        <div class="welcome">
            <% if (username) { %>
                <span>Welcome, <%= username %></span>
            <% } else { %>
                <button class="btnn">User</button>
            <% } %>
        </div>
        <br>
        <button class="btnn"><a href="/home">Back to Home</a></button>
    </div>
</body>
</html>

