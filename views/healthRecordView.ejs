<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Health Record - <%= user.name %></title>
    <link rel="stylesheet" href="/public/styles.css">
</head>
<body>
    <div class="header">
        <h1>Health Record for <%= user.name %></h1>
    </div>

    <div class="table-container">
        <h2>Health Record Details</h2>
        <table>
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Diagnosis</th>
                    <th>Treatment</th>
                    <th>Medications</th>
                </tr>
            </thead>
            <tbody>
                <% if (healthRecord) { %>
                    <tr>
                        <td><%= healthRecord.date || 'N/A' %></td>
                        <td><%= healthRecord.diagnosis || 'N/A' %></td>
                    <td><%= healthRecord.treatment || 'No notes available' %></td>
                    <td><%=healthRecord.medications ||'N/A' %></td>
                    </tr>

                <% } else { %>
                    <tr>
                        <td colspan="4" style="text-align: center;color: #888;">No health record available for this user.</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>
